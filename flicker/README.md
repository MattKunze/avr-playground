Simple flicker application for a dumb halloween *costume*.

Flickers LEDs on the output pins. PIN1 doesn't work unless you set the
reset disable fuse, which will prevent you from accessing the chip with
ISP
